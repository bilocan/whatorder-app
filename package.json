{
  "name": "whatorder",
  "private": true,
  "workspaces": ["backend", "dashboard"],
  "scripts": {
    "predev": "taskkill /F /IM ngrok.exe 2>nul & exit /b 0",
    "dev": "dotenv -e .env -- concurrently -n backend,dashboard,ngrok -c blue,green,yellow \"npm run dev --prefix backend\" \"npm run dev --prefix dashboard\" \"ngrok http --url=%NGROK_DOMAIN% 3000\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:dashboard": "npm run dev --prefix dashboard"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "dotenv-cli": "^11.0.0"
  }
}
