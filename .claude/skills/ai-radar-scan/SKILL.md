---
name: ai-radar-scan
description: Weekly AI tooling scan for WhatOrder (changelog + discovery). Use when the user says "ai radar scan", "radar scan", "run radar", or "run the ai radar scan".
---

When invoked, follow the prompt at:
`../../../../../whatorder-vault/Skills/ai-radar-scan/prompt.md`

That file is loaded via vault `CLAUDE.md` (through `.claude/CLAUDE.local.md`). Execute its workflow directly.
