Container surface. `light` is the white dashboard card (soft shadow); `dark` is the marketing panel (hairline border, optional hover brighten).

```jsx
<Card surface="light">Login form…</Card>
<Card surface="dark" hover>Feature step…</Card>
```
