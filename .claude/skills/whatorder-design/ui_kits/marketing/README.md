# Marketing site — UI kit

Recreation of the **dark** `whatorder.at` landing page, composed from the design-system
components.

**Sections:** sticky nav (logo + language switch + CTA) → hero (glowing mark, pilot badge,
gradient-emphasis headline, dual CTA) → how-it-works step grid → who-it's-for + testimonial →
early-access form → footer.

**Files**
- `index.html` — the full page; composes `BrandLogo`, `Badge`, `Button`, `SectionLabel`,
  `StepCard`, `QuoteBlock`, `Tag`, `Input` from `window.WhatOrderDesignSystem_b54bed`.

Copy is the English variant; the real site ships German (default), Turkish, and English.
