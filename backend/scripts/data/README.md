# WhatsApp template batch data

**Do not put real phone numbers in git.**

- Committed: `template-batch.example.csv` only (fake `+43000000000x` placeholders).
- Local only (gitignored): copy to `template-batch.csv` and edit that file.

```bash
cp scripts/data/template-batch.example.csv scripts/data/template-batch.csv
# edit template-batch.csv with real numbers — never commit it
```

Everything else under this folder is gitignored.
