# Agent skills (Cursor)

Workflow skills live in `.claude/skills/` (thin pointers to vault).

Do not add Firebase or Xcode skill packs here. See `.claude/skills/README.md` and `whatorder-vault/Projects/WhatOrder/specs/ai-config-architecture.md`.
